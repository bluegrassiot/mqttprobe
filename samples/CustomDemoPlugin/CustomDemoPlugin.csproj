<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net10.0</TargetFramework>
        <LangVersion>latest</LangVersion>
        <!-- Sample plugin: kept deliberately minimal for readers to copy, so
             library analyzers work against its purpose. -->
        <AnalysisMode>None</AnalysisMode>
    </PropertyGroup>

    <ItemGroup>
        <ProjectReference Include="..\..\src\MqttProbe.Shared\MqttProbe.Shared.csproj" />
    </ItemGroup>

</Project>
